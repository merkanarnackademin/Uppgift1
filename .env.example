# Example environment variables for the Blog Platform
# Copy this file to `.env` and adjust values as needed.

# SQLite database file (relative to project root)
# For dev, Prisma will create the file if it does not exist.
DATABASE_URL="file:./prisma/dev.db"

# Node environment (typically set by npm scripts automatically)
# Allowed values: development | test | production
NODE_ENV=development
